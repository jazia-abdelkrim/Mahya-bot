[phases.setup]
nixPkgs = ["python311", "pip"]       # install Python 3.11 and pip
[phases.install]
cmds = ["pip install -r requirements.txt"]   # install your bot's dependencies
[start]
cmd = "python bot.py"                # the command to actually run the bot